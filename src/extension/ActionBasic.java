package extension;

import client.IAJ;
import client.Game;

public class ActionBasic implements IAJ {
	@Override
	public void action(Game j) {
		// TODO Auto-generated method stub
	   /*j.deplacer("droite");
	   j.getHero().setPos(j.getHero().getX()+1, j.getHero().getY());
	   if(j.getHero().getX()>j.getMap().getLargeur())
		   return false;
	   j.afficher();
	   System.out.println();
	   return true;*/
	}
}
